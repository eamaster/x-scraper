/**
 * Local Development Configuration Template
 * 
 * This file is for LOCAL DEVELOPMENT ONLY.
 * For production, use the Cloudflare Worker proxy instead.
 * 
 * Instructions:
 * 1. Copy this file and rename it to 'config.js'
 * 2. Add your RapidAPI key to config.js
 * 3. NEVER commit config.js to version control (it's in .gitignore)
 * 4. In script.js, set WORKER_URL to null to use this config
 */

window.API_CONFIG = {
    key: 'YOUR_RAPIDAPI_KEY_HERE',
    host: 'twitter241.p.rapidapi.com'
};

