/**
 * Worker Configuration Template
 * 1) Copy this to 'worker-config.js' on the LIVE SERVER only.
 * 2) Set YOUR OWN Worker URL below.
 * 3) Do NOT commit worker-config.js (it's gitignored).
 */

window.WORKER_URL_OVERRIDE = 'https://YOUR_WORKER.subdomain.workers.dev';

