/**
 * Worker Configuration Template
 * 
 * This file is for PRODUCTION DEPLOYMENT.
 * 
 * Instructions:
 * 1. Copy this file and rename it to 'worker-config.js'
 * 2. Add your actual Cloudflare Worker URL to worker-config.js
 * 3. NEVER commit worker-config.js to version control (it's in .gitignore)
 * 4. Deploy worker-config.js to your live server (hesam.me)
 */

// Set your actual Cloudflare Worker URL here
window.WORKER_URL_OVERRIDE = 'https://twitter-api-proxy.smah0085.workers.dev';

